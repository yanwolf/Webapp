# 黑鑽策略回測 — 多使用者版（登入 + 各自的 Telegram Bot）

## 這次的改動

- **登入系統**：帳號密碼登入，需要邀請碼才能註冊（適合 10 人以內的小圈子使用）
- **每人一組 Telegram Bot**：不再共用同一組環境變數，每個人在網頁上貼上自己申請的 Bot Token，系統會自動幫忙註冊連線，不用再手動跑 curl 網址
- **資料互相隔離**：每個人的追蹤清單、Telegram 設定完全獨立，看不到彼此的資料
- 申請 Bot 的教學步驟直接寫在「追蹤清單」頁籤裡（點「不知道怎麼設定？」展開）

---

## 一、部署設定（Zeabur backend 服務的環境變數）

```
JWT_SECRET_KEY=自己隨便設一組夠長的亂數字串（例如用密碼產生器生一組32字元的）
SIGNUP_INVITE_CODE=你想給這10個人的邀請碼，例如 heijuan2026
PUBLIC_BACKEND_URL=https://你的後端網域.zeabur.app
DB_PATH=/app/data/app.db
```

**JWT_SECRET_KEY 很重要**：這是用來簽發登入憑證的密鑰，沒設定的話系統會用一個不安全的預設值，而且每次重新部署都會變（所有人都要重新登入）。正式使用前務必設定成一組你自己保密的亂數字串。

**PUBLIC_BACKEND_URL** 是讓系統知道「自己的網址是什麼」，這樣使用者貼 Bot Token 進來時，才能自動幫他註冊 Telegram webhook（不用再手動跑 curl 網址那一步）。

## 二、加掛 Volume（跟之前一樣，必須做）

backend 服務設定裡找 **Volume**，掛載到 `/app/data`。沒加的話帳號、追蹤清單、Bot 設定每次重新部署都會消失。

## 三、給使用者的使用流程（已經寫在網頁上了）

1. 你（管理員）把網站網址 + 邀請碼發給最多 10 個人
2. 每個人自己到網站「註冊新帳號」，輸入帳號密碼 + 邀請碼
3. 登入後切到「追蹤清單」頁籤
4. 展開「不知道怎麼設定？」教學，跟著步驟去 Telegram 找 @BotFather 申請一個屬於自己的 Bot，拿到 Bot Token
5. 把 Token 貼到網頁上，按「儲存並啟用」——系統自動處理好連線設定
6. 回 Telegram 搜尋自己剛建立的 Bot，傳送 `/start` 完成最後連結
7. 新增想追蹤的標的（市場/代碼/策略/K線週期），之後有新訊號就會收到自己 Bot 傳來的通知

## 四、技術細節（給你參考）

- 密碼用 bcrypt 雜湊儲存，不會存明文
- 登入憑證用 JWT，有效期 30 天
- 每個人的 Telegram webhook 網址都不同（網址裡包含一組隨機字串當作身份識別），Telegram 傳訊息進來時系統靠這組網址就知道是哪個使用者，不會搞混
- 背景排程掃描「所有使用者、所有啟用中」的追蹤項目，依各自的K線週期決定檢查頻率，通知時查該筆項目屬於哪個使用者、用哪個使用者自己的 Bot 發送

## 五、已知限制

- 邀請碼是共用同一組（不是每人一組一次性邀請碼），如果外流，任何知道邀請碼的人都能註冊——對 10 人以內的小圈子這樣的安全等級是足夠的，如果你想要更嚴格的控管（例如逐一審核、或用完即失效的邀請碼），跟我說可以再加強
- 多副本部署會造成背景排程重複執行、重複通知——目前僅適合單一副本
- W底/M頭訊號有confirmation delay（策略本身特性，非bug）
- 尚未支援自動下單，只做通知
